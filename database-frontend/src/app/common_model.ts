export enum Gender {
  MALE = "male",
  FEMALE = "female"
}

export enum ViewMode {
  VIEW, EDIT
}

export enum Amount {
  CRITICAL = "critical_amount",
  AVERAGE = "average_amount",
  ENOUGH = "enough_amount"
}